# Ночная итерация 24.09.2026

Ветка `night/2026-09-24`. `main` и превью не трогаются. Утром — пул-реквест с отчётом.
Правила: всё по `CLAUDE.md`, один коммит на задачу, сборка должна проходить перед каждым коммитом.

## Задачи

- [x] 1. Страница 404 в стиле сайта
- [x] 2. Favicon (SVG + PNG для iOS) и OG-картинка 1200×630 для соцсетей
- [ ] 3. sitemap.xml, robots.txt, .htaccess для Бегета (https, кеш, сжатие, 404)
- [ ] 4. Кейсы: скриншоты сайтов клиентов в карточки, логотипы в хорошем качестве
- [ ] 5. Страницы услуг по четырём секторам колеса, переход по клику на сектор
- [ ] 6. Аудит доступности и скорости (Lighthouse), исправления
- [ ] 7. Финал: сборка, push ветки, пул-реквест с отчётом

## Отчёт

### 1. Страница 404
- Сделано: `src/pages/404.astro` — карточка с eyebrow «Ошибка 404», заголовком «Этой страницы нет в нашем цикле» и двумя кнопками: на главную и в Telegram. GitHub Pages и Бегет отдают `404.html` сами.
- Попутно: логотип ARDA в футере в тёмной теме был белой плашкой — теперь инвертируется.
- Проверить: открыть любой несуществующий адрес, например `/emilius-site/net-takoy`.
- Решение владельца: не требуется.

### 2. Favicon и картинка для соцсетей
- Сделано: `favicon.svg` (знак со спектральным градиентом), `favicon-32.png` для старых браузеров, `apple-touch-icon.png` 180×180 для iOS. Картинка `og.jpg` 1200×630: логотип, миссия, колесо — отрисована из того же SVG, что на сайте, в светлой теме. В `<head>` добавлены og:image, og:locale, twitter:card.
- Проверить: отправить ссылку на превью в Telegram — должна появиться карточка с колесом. Вкладка браузера — со знаком.
- Решение владельца: не требуется. Если захотите другой текст на картинке — перерисую за минуту.
