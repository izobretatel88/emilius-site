# Передача дел — сайт «Эмилиус Эдженси»

Состояние на 25.09.2026. Этот файл — вход для новой сессии: здесь всё, что было решено и сделано в первой длинной сессии, и что делать дальше.

## С чего начать новую сессию

1. Прочитать этот файл и `CLAUDE.md`.
2. Пул-реквест `dev` → `main` открыт: https://github.com/izobretatel88/emilius-site/pull/2. Вливать — только после «да» владельца.
3. Спросить владельца, с чем продолжаем (список открытых вопросов — ниже).

## Где что

| Что | Где |
|---|---|
| Репозиторий | https://github.com/izobretatel88/emilius-site (публичный, аккаунт `izobretatel88`) |
| Локальная папка | `~/Documents/emilius-site` |
| Рабочее превью (`main`) | https://izobretatel88.github.io/emilius-site/ |
| Черновик (`dev`) | https://izobretatel88.github.io/emilius-site/dev/ — в углу метка «черновик · ветка dev» |
| Заставку показать снова | добавить `?intro` к адресу |
| Старый сайт на Tilda (ещё живой) | https://emilius.agency |
| Дизайн-система | `docs/EMILIUS-DESIGN-SYSTEM.md`, токены — `src/styles/tokens.css` |
| Холст Pencil | `design/system.pen` |
| Шрифт Glober | `src/styles/fonts/` (woff2, 400/600/700/800), подключение — `src/styles/fonts.css`. Исходные TTF у владельца (`Font Glober.zip`), в репозиторий не кладём |

## Как устроен деплой

- `.github/workflows/deploy.yml` запускается из `main` и собирает **обе** ветки: `main` в корень превью, `dev` — в подпапку `/dev/`.
- Пуш в `dev` запускает `dev-trigger.yml`, который дёргает деплой из `main` (окружение `github-pages` разрешает публикацию только из `main`, настройки репозитория не трогали).
- Превью собирается с `PREVIEW=1`: `noindex` + `robots.txt` с `Disallow: /`, чтобы не конкурировать с emilius.agency в поиске.
- GitHub Pages раздаёт новую версию с задержкой до ~2 минут после успешного деплоя (кеш `max-age=600`). Проверять с `?n=<время>` в адресе.
- Продакшен позже — Бегет: `SITE_URL=https://emilius.agency BASE_PATH=/ npm run build`, содержимое `dist/` (вместе со скрытым `.htaccess`) в корень хостинга. Подробно — `README.md`.

## Процесс работы с владельцем

- Эксперименты — в ветке `dev`, смотрим на `/dev/`, понравилось — вливаем в `main`.
- Мелкие утверждённые правки можно сразу в `main` (так делали: контраст, www, миссия).
- Pencil: владелец правит холст → **⌘S** (без сохранения на диске старая версия) → говорит «переноси» → читаем холст через MCP, переносим в компоненты Astro и токены, пушим. Выгрузку Tailwind из Pencil не используем как код.
- Перед пушем: `npm run build`. Для проверок в браузере — дев-сервер через `preview_start` с именем `emilius-site` из `.claude/launch.json`.

## Решения владельца (не пересматривать без запроса)

- Адрес без www: `https://emilius.agency`.
- Кнопки залиты `#008385` (`--teal-btn`), мелкий бирюзовый текст — тоже `#008385` (`--teal-text`); фирменный `#00999B` — рамки, колесо, крупные акценты. Доступность 100.
- Eyebrow («НАША МИССИЯ») — жирность 800, на всех страницах.
- Обод центрального диска колеса — тонкий, 1.1.
- Миссия на главной — в две строки, перенос после «создавать» (на телефоне переносится сама).
- **Основной знак** — скруглённый треугольник со спектральной обводкой, внутри пастельная заливка и серое сердце `#9A9FA0` (заливка скрыта в тёмной теме). Генератор — `src/lib/mark.mjs`; после правки знака: `npm run build && npm run brand`.
- **Знак-молния** — второй знак, на сайте не используется. Рабочие файлы — `design/logo-bolt/`. Пропорции — как в исходнике Figma (`Group 30`); **сплющенная квадратная версия отвергнута** («ужасно смотрится»). Выбрано: плавный вертикальный градиент, линия 20 (`bolt-line.svg`), для иконок 24.
- Звук заставки — файл владельца `public/brand/intro.mp3` (владелец подтвердил разрешение на использование, 25.09.2026). Синтезированный звук (`sting.ts`) удалён.
- Заставка повторяет эталонное видео владельца кадр в кадр (`CleanShot 2026-09-23 at 18.03.37.mp4`). Тайминги — константы `T` в `Intro.astro`.

## Что лежит в `dev` и ещё не в `main`

1. **Знак с пастельной заливкой и плотным сердцем** — таблетка логотипа, иконка iOS, `logo-mark.png`, картинка для соцсетей.
2. **Знак-молния** — `design/logo-bolt/` (переносимый мастер из Figma, мелкая версия, версия с плавным цветом). На сайт не влияет.
3. **Колесо 2.0** (`src/components/Wheel.astro`):
   - точка текущей недели и дуга пройденной части года на ободе, подпись «Сегодня неделя N · идёт этап · пройдено X%»;
   - при загрузке колесо приезжает на сегодняшнюю неделю, дальше медленное вращение (156 с);
   - текущий этап насыщеннее; наведение — сектор проявляется, в центре название этапа, под колесом описание и «Подробнее»; колесо стоит;
   - касание: первое выбирает и доворачивает этап под стрелку, второе открывает страницу; клавиатура: Tab/стрелки/Enter/Esc;
   - выбор с клавиатуры/касанием главнее наведения мыши (иначе под неподвижным курсором проезжали сектора).
4. **Заставка** (было «Дай пять!», теперь по эталонному видео, старт «Со звуком / Без звука» с таймером 5 с) (`src/components/Intro.astro`, `src/lib/sting.ts`, `public/brand/mark.svg`):
   - «Добро пожаловать!» + объёмный знак + ладонь ✋ в радужном круге «Готовы? Дай пять!»;
   - нажатие в любом месте / Enter / пробел — хлопок, кольцо, звук удара сразу (без разгона), наклон знака с бликом, сайт;
   - Esc или прокрутка — пропуск без звука; один раз за визит (`sessionStorage`), при «уменьшить движение» не показывается.
5. **Форма «Обратная связь» → Telegram** (`src/components/Feedback.astro`, `public/api/lead.php`). Работает только на Бегете; настройка бота — README.
6. **Шрифт Glober** вместо Manrope (лицензия у владельца), `og.jpg` пересобрана.

## Ночь 25.09.2026

Итеративная работа в `dev` (цель: Lighthouse 95+ везде, чистая консоль, 375 px без прокрутки вбок, тёмная тема, клавиатура, тексты). Замеры: `npx lighthouse@12` по сборке `npx astro preview --port 4322`.

- Старт: компьютер 100/100/100/100; телефон главная — скорость 94; этапы — доступность 95 (контраст `--ink-3`).
- `--ink-3` поднят: светлая `#686d6c`, тёмная `#808d8a` — 4,5:1 на фоне и в полях. Этапы → доступность 100.
- CSS встроен (`inlineStylesheets: 'always'`), preload Glober 400/600 в `Base.astro`. Телефон, главная: скорость 94 → 97 (LCP 2,5 с — заголовок; остаток — время ответа и подмена шрифта).

## Открытые вопросы владельцу

- Влить `dev` в `main` (после просмотра PR https://github.com/izobretatel88/emilius-site/pull/2).
- Тексты страниц этапов — черновик без цифр и обещаний: `src/data/services.ts`, особенно касдев и маркетинг; у касдева нет кейса.
- Логотип Geekboards — взят их текущий чёрный вместо прежнего синего; подтвердить.
- Форма обратной связи — сделана в `dev` (бот Telegram через `api/lead.php`). Осталось владельцу: создать бота, положить `emilius-telegram.php` на Бегет (README).
- Переезд на Бегет и перенос домена.
- Виджет Эми (Millis) на localhost рисуется чёрным прямоугольником — проверить на живом превью.
- Знак-молния для мелких размеров: пропорции остаются узкими; для favicon/аватарок — вариант на тёмном круге.

## Грабли и как их обходить

- **Pencil / pen.dev**: `.pen` читать и менять только через MCP `pencil` (файлы зашифрованы). Работает только с файлом, открытым в приложении. SVG как заливку не показывает — PNG логотипов клиентов лежат в `design/assets/projects/`. Импорт целой страницы из браузера зависает — импортировать частями (`header.hero`, колонки по отдельности). После правок через MCP файл «Edited» — просить владельца нажать ⌘S.
- **Claude Code CLI** для Pencil-агента установлен в `~/.local/bin/claude` (официальный установщик), вход выполнен через аккаунт claude.ai.
- **GitHub CLI** авторизован (`gh auth status`). Интерактивные запросы в панели терминала у владельца не принимают ввод — если понадобится, отвечать через `expect` (так проходили `gh auth login`).
- **Панель браузера**: если вкладка в фоне (`document.visibilityState === 'hidden'`), браузер замораживает анимации и `requestAnimationFrame` — проверки колеса и заставки дают ложные «зависания». Перед проверкой анимаций — `tabs_select`, смотреть `visibilityState`.
- **Дев-сервер vs сборка**: дев-сервер иногда держит старые стили в кеше вкладки — сверять по собранной версии (`npx astro preview --port 4322`).
- **macOS**: нет утилиты `timeout`; zsh не разбивает строки по словам в `for` — перечислять аргументы явно.
- **Звук**: браузеры не играют звук без действия человека — поэтому «Дай пять»; без клика `playSting` сам отказывается (контекст «suspended»), чтобы звук не заиграл невпопад.

## Как владелец предпочитает работать

- Пишет по-русски, коротко, часто с опечатками — понимать по смыслу.
- Любит видеть варианты картинкой рядом (крупно, 32 и 16 px, на тёмном) и получать рекомендацию, а не список опций.
- Ценит инициативу: «на свой взгляд», «покажи, на что способен» — делать смело, но в `dev`.
- Отвергнутое не возвращать (сплющенная молния; кнопки «Войти» и «Дай пять» заменены выбором «Со звуком / Без звука» с таймером, 25.09.2026).
